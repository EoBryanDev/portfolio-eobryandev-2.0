## ADDED Requirements

### Requirement: Strict Dark Mode
The application SHALL enforce a strict Dark Theme across all pages with no toggle for Light Mode.

#### Scenario: User visits the site
- **WHEN** the website loads
- **THEN** the UI is rendered exclusively in Dark Mode regardless of system preferences
